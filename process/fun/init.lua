local fun = require(".fun.fun")

-- local crypto = {
--     _version = "0.0.1",
--     digest = digest,
--     utils = util,
--     cipher = cipher,
--     random = cipher.issac.random,
--     mac = mac,
--     kdf = kdf,
-- };

return fun